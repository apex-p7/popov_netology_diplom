apiVersion: v1
kind: Service
metadata:
  name: static-site
  namespace: sample-app
spec:
  type: LoadBalancer
  selector:
    app: static-site
  ports:
    - name: http
      port: 80
      targetPort: 80
